{
  "name": "aust-hub-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/inline-css.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.1.2"
  }
}
